SimpleGame: use Deck to track cards(or any obj) ==>
    cards can only be created at Deck,
    Deck give each card an id, the id should sync
    between server and clients.

    when revealing, use id to ensure the correctness.
    out of sync -> raise GameError
    
CommonUI:
ARTWORKS!

Core:
user subsystem,
error reporting subsystem
    * server auto restart
auto upgrade subsystem

make robust


and finally, the game!
